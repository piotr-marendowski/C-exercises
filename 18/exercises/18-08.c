/* License: Creative Commons Attribution 4.0 International License
Author: William Gherman
Purpose: Exercise 8
Modification date (DD/MM/YYYY): 07.12.2022 */

#include <stdio.h>

int main (void)
{
    /*
    (a)

    x is an array of ten pointers to functions with an int argument which return char.
    
    (b)

    x is a function which takes an int argument and returns a pointer to an array of five integers.
    
    (c)

    x is a function with no arguments which returns a pointer to a function -- which has an int argument and returns a pointer to a float.
    
    (d)

    x is a function with two arguments: an int and a pointer to a function with an int argument which returns nothing. x returns a pointer to a function with an int argument and no return value.
    */

    return 0;
}